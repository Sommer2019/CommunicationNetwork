FROM python:3.12-slim

WORKDIR /app

# Abhängigkeiten installieren
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Watchdog-Script kopieren
COPY failover_watchdog.py .

CMD ["python", "-u", "failover_watchdog.py"]
